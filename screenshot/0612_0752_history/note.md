# Screenshot note — History 0612_0752

Công cụ chụp ảnh trong phiên này KHÔNG lưu được file ảnh ra đĩa (`save_to_disk` không có hiệu lực),
nên thư mục này chỉ chứa ghi chú mô tả các màn hình quan trọng đã quan sát.

## Các màn hình / phát hiện
1. **Thanh lọc Lịch sử** — chỉ có 3 tab: `Tất cả` / `Tại bàn` / `Mang về`. KHÔNG có tab `Đặt online` (FIND-01).
2. **Tab "Mang về"** — danh sách rỗng, hiển thị "Không tìm thấy đơn hàng nào".
3. **Dropdown "Tất cả bàn"** — danh sách bàn bị lặp mục `ban 1` và `ban 2` (FIND-03).
4. **Lọc bàn = ban7** — Tổng 2 đơn: thẻ `ban7` (POS00002472CN2, Đã hủy) + thẻ `ban kiem tra ten dai`
   (POS00002471CN2, Chưa thanh toán). Xuất hiện đơn của bàn khác khi lọc ban7 (FIND-02, nghi do gộp bàn).
